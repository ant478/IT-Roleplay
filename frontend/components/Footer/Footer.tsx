import * as React from 'react';

export default class Footer extends React.Component {
  public render(): React.ReactNode {
    return (
      <footer className="footer">
        ant478, all right reserved
      </footer>
    );
  }
}
